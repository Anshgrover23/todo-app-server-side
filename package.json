{
  "name": "project_3",
  "version": "1.1.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
  "postinstall": "npx prisma generate",
  "dev": "npx prisma generate && node --watch --env-file=.env --experimental-strip-types --experimental-sqlite ./src/server.js",
  "start": "prisma generate && node ./src/server.js",
  "test": "echo \"Error: no test specified\" && exit 1"
},
  "keywords": [],
  "author": "",
  "license": "MIT",
  "description": "A dockerized full-stack todo-application that uses Node.js as a backend, PostgreSQL as a database,a Prisma ORM, and JWT Authentication.",
  "dependencies": {
    "@prisma/client": "^6.0.1",
    "bcryptjs": "^2.4.3",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.13.1",
    "prisma": "^6.0.1"
  }
}
